<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <!-- Global Settings -->
    <property name="severity" value="error"/>

    <!-- Checks for coding conventions -->
    <module name="TreeWalker">

        <!-- Suppress documentation requirements -->
        <module name="JavadocType">
            <property name="scope" value="protected"/>
            <property name="severity" value="ignore"/>
        </module>
        <module name="JavadocVariable">
            <property name="scope" value="protected"/>
            <property name="severity" value="ignore"/>
        </module>
        <module name="JavadocMethod">
            <property name="scope" value="protected"/>
            <property name="severity" value="ignore"/>
        </module>

        <!-- Class design checks -->
        <module name="VisibilityModifier"/>

        <!-- Avoid public constructors in utility classes -->
        <module name="HideUtilityClassConstructor"/>

        <!-- Naming conventions -->
        <module name="LocalFinalVariableName"/>
        <module name="MemberName"/>

        <!-- Restrict line length to 120 characters -->
        <module name="LineLength">
            <property name="max" value="120"/>
            <property name="severity" value="warning"/>
        </module>

        <!-- Code style checks -->
        <module name="WhitespaceAfter"/>
        <module name="WhitespaceAround"/>
        <module name="NeedBraces"/>

        <!-- Prevent magic numbers -->
        <module name="MagicNumber">
            <property name="ignoreNumbers" value="0, 1, -1, 100, 120, 2000"/>
            <property name="severity" value="warning"/>
        </module>

        <!-- Simplify error handling -->
        <module name="FinalParameters"/>

        <!-- Limit nested blocks for readability -->
        <module name="NestedIfDepth">
            <property name="max" value="3"/>
        </module>
        <module name="NestedTryDepth">
            <property name="max" value="2"/>
        </module>

        <!-- Code clarity -->
        <module name="AvoidInlineConditionals"/>
        <module name="SimplifyBooleanExpression"/>

    </module>

    <!-- Miscellaneous checks -->
    <module name="FileLength">
        <property name="max" value="1000"/>
    </module>
</module>
